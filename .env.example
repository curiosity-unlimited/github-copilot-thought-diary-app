```bash
# Application Settings
SECRET_KEY=your_secret_key_here
ENV=development  # development, testing, or production

# SQLite Settings (Development)
SQLITE_PATH=thought_diary.db

# PostgreSQL Settings (Production)
DB_USER=postgres
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=thought_diary

# Security Settings
JWT_SECRET_KEY=your_jwt_secret_key_here
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRES_DAYS=30

# Rate Limiting Settings
# For production, use Redis for rate limiting
# REDIS_URL=redis://localhost:6379/0

# GitHub API Settings
# Create a personal access token with 'models: read' permission
# Visit: https://github.com/settings/tokens
GITHUB_API_KEY=your_github_api_key_here
GITHUB_MODEL=openai/gpt-4o  # Model ID for GitHub inference, format: {publisher}/{model_name}
GITHUB_MAX_TOKENS=1000      # Maximum number of tokens for response generation